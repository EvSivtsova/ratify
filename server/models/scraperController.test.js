const ScraperApi = require('../controllers/scraperController');

describe('ScraperApi tests for both Events and Food',() => {
  describe('events',() => {
    it('first instance of returned result is correct',() => {
      
    })
  })
})